from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from core.animation import AnimationController
    from core.types import EntityState, Direction, ItemCategory, ToolType, Material, Quality, FontType


